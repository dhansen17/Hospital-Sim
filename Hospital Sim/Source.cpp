#include<iostream>
#include<string>
#include"Simulator.h"

using namespace std;

//having problems with seeing the hospital class if we saw that then the program should run effectively, and knowing my luck, we are probly not seeing it because I am really bad at spelling
int main()
{
	Simulator sim;
	sim.showSim();

	return 0;
}